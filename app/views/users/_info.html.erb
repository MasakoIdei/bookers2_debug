<table class='table'>
  <tr><%= attachment_image_tag(user, :profile_image, size: "100x100", fallback: "no_image.jpg") %></tr>
  <tr>
	  <th>name</th>
	  <th><%= user.name %></th>
  </tr>
  <tr>
	  <th>introduction</th>
	  <th><%= user.introduction %></th>
  </tr>
  <tr>
	  <th>follows</th>
	  <th>
	    <%= link_to  user_followings_path(user.id) do %>
	      <%=user.followings.count %>
	    <% end %>
	  </th>
	  
  </tr>
  <tr>
	  <th>followers</th>
	  <th>
	    <%= link_to  user_followers_path(user.id) do %>
	      <%= user.followers.count %></th>
	    <% end %>
  </tr>
  
</table>
<!--変更前の@book.user.id ==current_user.idだと左側の比べたいidが都度変わってしまう可能性がある-->
<!--その時にuser_info表示されているユーザーとカレントユーザーが一緒であるかどうか？？を比べたいので-->
<!--user.id == current_user.idとなる-->
<% if user.id == current_user.id %>
　<div class='row'>
  　<%= link_to '',edit_user_path(user),class: "btn btn-outline-secondary btn-block fas fa-user-cog edit_user_#{user.id}" %>
　</div>
<% else %>
  <div>
   <%= render 'relationships/follow_button', user: user%>
  </div><br>
<% end %>　